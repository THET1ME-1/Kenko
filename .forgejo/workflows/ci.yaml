name: CI

on: [push]

jobs:
  test:
    runs-on: codeberg-small
    steps:
      - uses: actions/checkout@v4
      - uses: setup-gradle
      - name: Run unit tests
        run: ./gradlew testDebugUnitTest
